# Copy to .env.local — never commit .env.local

NEXT_PUBLIC_API_URL=https://api.gyansanchaar.cloud/api/v1
NEXT_PUBLIC_APP_URL=https://gyansanchaar.cloud

# Internal API calls (server-side only, not exposed to browser)
API_INTERNAL_URL=https://api.gyansanchaar.cloud/api/v1
